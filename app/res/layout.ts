/* eslint-disable no-unused-vars */

namespace Layout {
  export enum Spacing {
    XS = 5,
    SM = 10,
    MD = 20,
    LG = 30,
    XL = 50,
  }

  export enum BorderRadius {
    SM = 7,
    PILL = 100
  }
}

export default Layout;
