/* eslint-disable no-unused-vars */

namespace Layout {
  export enum Spacing {
    SM = 10,
    MD = 20,
    LG = 30
  }

  export enum BorderRadius {
    SM = 10,
    PILL = 100
  }
}

export default Layout;
